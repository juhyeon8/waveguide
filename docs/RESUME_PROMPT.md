# 다음 세션 시작 프롬프트

> 아래 블록을 새 Claude Code 세션에 그대로 붙여넣으세요.
> (작업 폴더 `C:\dev\sixth-task`에서 시작)

---

```
도선 배열 도파관 시뮬레이션 구현을 이어서 하려고 해.

먼저 이 세 파일을 순서대로 읽어서 맥락을 잡아줘:
1. HANDOFF.md — 현재 상태·불변 규약·파일 지도 (여기부터)
2. docs/superpowers/plans/2026-07-08-도선도파관-simulation.md — 구현 계획(실제 코드 포함, 10개 태스크)
3. docs/specs/2026-07-08-도선도파관-design.md — 확정 스펙

읽고 나서, 구현 시작 전에 아래를 확인해줘:
- git 상태(git status)와 마지막 커밋(git log --oneline)으로 어디까지 됐는지
- node core.js 를 돌려 물리 코어 selfTest가 여전히 PASS인지

그다음 계획서 Task 1부터 TDD 순서(실패 테스트 → 구현 → 통과 → 커밋)로 진행해줘.
각 태스크 끝에서 결과를 보고하고 멈춰서, 내가 확인한 뒤 다음으로 넘어가자.

지킬 것:
- core.js(= wire_waveguide_core.js) 원본 수정 금지, kappaFitWindow 규칙 변경 금지
- 차단 영역(λ>2a)에서 c₁ 위상 수치·"위상차" 문구 금지 (동위상+감쇠만)
- 위상자 부호는 c.re·cosφ + c.im·sinφ (장 렌더와 동일, A6 확정)
- 투과율·SWR·스윕은 평면파 전용(A1·A2), 입출구 |c₁|은 기하평균(A3), a 슬라이더 step=2(C2)
- reference/ 값을 새로 짓지 말고 재사용(A7). reference의 computeFitInterval·computeModeField는 쓰지 말 것
- 물리 수치는 node로 자동 검증, 렌더·애니메이션은 브라우저 육안 체크리스트
- 응답은 한국어 존댓말, 되돌리기 어려운 작업(커밋 등)은 계획서대로 진행하되 문제 있으면 알려줘

실행 방식은 (1) 서브에이전트 태스크별 or (2) 인라인 배치 중에 내가 고를게. 먼저 위 3개 문서 읽고 현재 상태부터 보고해줘.
```

---

## 참고: 실행 방식 두 가지

프롬프트를 넣으면 Claude가 문서를 읽고 현재 상태를 보고합니다. 그 뒤 이렇게 고르면 됩니다:

- **"서브에이전트 방식으로 해줘"** — 태스크마다 새 에이전트, 사이사이 검토. 빠른 반복(권장).
- **"인라인으로 해줘"** — 이 세션에서 체크포인트 두고 배치 실행.

## 참고: 육안 확인이 필요한 순간

Task 5부터 브라우저 확인이 나옵니다. 그때 `C:\dev\sixth-task\index.html`을 **더블클릭**해서 열고, 계획서에 적힌 체크리스트대로 보면 됩니다. (서버 불필요 — file:// 실행)
