# 구현 지시 — higher-order 도선배열 시뮬 2×2 개편 + 렌더링/판독 수정

> 대상 폴더: `higher-order/`
> 수정 파일: `index.html`, `style.css`, `script.js`, `graph.js`, `render.js`
> **`../core.js` 원본 수정 절대 금지** (검증된 물리 코어, `kappaFitWindow` 규칙 불변)
> 물리 판단은 이미 확정됨. 이 문서는 구현 지시서이며, 물리 규약을 어기는 "개선"은 하지 말 것.

---

## 0. 물리 규약 (최상위 불변 — 어기면 롤백)

1. **인과 방향**: 경계조건이 산란파(도선 전류)를 강제 → 상쇄·차단은 그 *결과*. 역전 문장·라벨 금지.
2. **전체장 = 입사장 + 산란장.** 이 관계가 2×2 레이아웃의 핵심 메시지다. 세 히트맵은 이 덧셈이 눈으로 보이도록 만든다.
3. **용어**: 도선이 내는 파 = "산란파", evanescent = "감쇠파", 차단 = "차단(cutoff)". "복사파"·"증발장" 금지.
4. **차단 영역(k<k_c) 모드의 "위상"을 그래프·라벨·수치로 절대 쓰지 말 것.** 동위상 + 감쇠만. (전파 모드의 k_z 위상 측정은 허용.)
5. 입사파는 소스 도선의 원통파(`incLine`, H₀). 평면파 아님. (§4에서 확정: 선원파가 영상법과 정합.)

---

## 1. 구조 개편 — 2×2 레이아웃

### 1-1. 레이아웃
```
┌──────────────────┬──────────────────┐
│ 좌상: 입사 (inc)  │ 우상: 전체 (tot)  │   ← 현재 보이던 것
├──────────────────┼──────────────────┤
│ 좌하: 산란 (scat) │ 우하: 모드 그래프  │   ← 현재 cvGraph
└──────────────────┴──────────────────┘
```
- 시선 흐름(좌→우)으로 "입사 + 산란 = 전체"가 읽히도록. 좌열 위아래(입사·산란)를 더하면 우상(전체)이 된다는 배치.
- 각 히트맵 위에 작은 제목: "입사파 (소스 도선)", "산란파 (벽 도선)", "전체장 = 입사 + 산란".

### 1-2. 데이터는 이미 있음
`modes.js`의 `computeScene`이 이미 `inc`, `scat`, `tot` 세 필드를 **모두** 반환한다. 지금은 `tot`만 그리고 있을 뿐. 새 캔버스 `cvInc`, `cvScat`를 추가하고 `frame()`에서 각 필드에 `drawField`를 호출하면 된다.

### 1-3. HTML/CSS
- `index.html`: `cvTot` 한 개 → `cvInc`·`cvTot`·`cvScat` 세 캔버스 + `cvGraph`를 2×2 그리드로.
- 각 히트맵은 `.cvwrap`으로 감싸 오버레이 라벨(§3-2, §3-3)을 받을 수 있게 한다. (현재 `updateOverlays`가 `.cvwrap` 안 `.cv-label` span을 요구함.)
- `style.css`: 2×2 CSS Grid. 좁은 화면에선 1열로 흐르도록 반응형.

---

## 2. 세 히트맵 공통 색 스케일 (핵심)

**목적**: "입사 + 산란 = 전체"가 눈으로 더해지려면 세 패널이 **같은 스케일**을 써야 한다. 각자 자기 최대값에 맞추면 크기 관계가 깨진다.

### 구현
- 공통 스케일 = **`tot` 기준**, 현재 `autoScale`과 동일한 방식(관찰 구간 앞부분 `xLeft` ~ `xLeft+0.4L`의 최대 |E|).
- **`inc` 최대값을 스케일로 쓰지 말 것.** 소스 도선(H₀ 원통파)은 r→0에서 로그 발산 → 소스 근처 한 점이 스케일을 폭발시켜 나머지가 다 하얗게 죽는다.
- `frame()`에서 `sc = autoScale(s.tot)`를 **한 번** 계산해, 세 `drawField(g, s.inc, sc, phase)` / `drawField(g, s.tot, sc, phase)` / `drawField(g, s.scat, sc, phase)`에 **같은 sc** 전달.
- 결과: 입사 패널 소스 근처는 스케일을 넘어 포화(완전 빨강/파랑)될 수 있음 — 이는 "여기가 소스다"를 보여주므로 허용.

### 검증
- 프리셋 ①(완전차단): 산란 패널이 도파관 내부에서 입사 패널을 **상쇄**해, 전체 패널 내부가 눈에 띄게 약해지는(색이 옅어지는) 게 보여야 함. 세 패널 색 농도가 물리적 덧셈과 일관될 것.

---

## 3. 렌더링 수정 (render.js 중심)

### 3-1. 히트맵 감마 보정 — 색 진하게
현재 `colorForValue(v, scale)`는 `t = v/scale` 선형. 약한 장이 거의 안 보임.
- **수정**: `t`의 크기에 감마 0.6 적용하되 **부호 보존**:
  ```js
  var t = v / scale;
  var sign = t < 0 ? -1 : 1;
  var mag = Math.min(1, Math.abs(t));
  t = sign * Math.pow(mag, 0.6);
  ```
  이후 기존 빨강(+)/파랑(−) 매핑 그대로.
- **감마 0.6 고정.** (물리량 아님, 시각 표현.) 구현 후 육안으로 너무 진하면 0.7, 너무 약하면 0.5로 한 단계만 조정 가능 — 그 외 값은 쓰지 말 것.
- **부호 보존이 필수**: 감쇠(상쇄)가 색의 약해짐으로 정직하게 보여야 하므로 |v|만 키우고 부호를 잃으면 안 됨.

### 3-2. 도선 벽 위치 명확화
- `drawWireDots`는 이미 도선 점을 찍음(유지).
- 추가: 두 벽 라인(y = y0pix ± a/2)을 얇은 가이드선으로. `drawPlatesWire`가 이미 내부 밴드를 반투명 칠하고 있으니, 벽 위치에 1px 실선을 얹어 경계를 또렷하게.
- `updateOverlays`의 '도선 벽' 라벨(상·하)이 세 히트맵 모두에 뜨도록 `.cvwrap` 구조 연결.

### 3-3. '입구' 라벨 — 빨간 세로선의 정체
- 사용자가 의문을 가진 좌측 세로선 = `drawPlatesWire`의 **도파관 입구(z=0, 픽셀 x=xLeft) 안내선**이다. (소스 도선 아님.)
- `updateOverlays`에 이미 '입구' 라벨 코드가 있음 → `.cvwrap` 연결로 실제 표시되게 살릴 것.
- 라벨 텍스트: "입구 (z=0)". 이 세로선이 그래프의 z=0과 같은 지점임을 사용자가 알 수 있게.

### 3-4. 그래프 y축 라벨 (graph.js)
- 현재 `drawModeGraph`에 x축("z (진행축)")만 있고 y축 제목 없음.
- 추가: y축을 따라 세로 회전 텍스트 **`|cₙ(z)|  (모드 계수 크기, 로그)`**.
- 물리 의미: 각 횡방향 모드 sin(nπy/a) 성분의 진폭. 판독값 문구와 용어 일치시킬 것.

### 3-5. 실선/점선 범례 시각화 (graph.js `drawLegend`)
- 현재 "실선=실측(MoM) 점선=이론"이 텍스트로만 있음.
- 수정: 범례에 **실선 샘플 획**과 **점선 샘플 획**을 실제로 그려서 나란히 표시.
  예: `[실선 획] 실측(MoM)`  `[- - 점선 획] 이론`.
- 모드 색(1=파랑,2=초록,3=주황) 범례는 유지.

### 3-6. 그래프 수치 바닥 아래 실측선 흐리게 (graph.js)
- **배경**: 강차단 모드(mode3 등)는 e^{−κz}로 감쇠하다 수치 바닥(~1e-4)에 닿으면 그 아래는 의미 없는 노이즈 톱니. 현재 z=0..L 전 구간을 같은 굵기로 그려서 "이론과 안 맞는다"는 오해를 줌.
- **수정**: 실측 실선을 그릴 때, 값이 `norm * 1e-4` (= 그래프 하단 1e-4 데케이드) 아래로 내려가면 그 구간은 **반투명(globalAlpha ≈ 0.25)**으로. 측정 가능 구간(이론 점선과 겹치는 곳)은 불투명 유지.
- **효과**: "이론과 잘 맞는 구간"과 "수치 바닥 노이즈 구간"이 분리되어 검증력이 정직하게 드러남. §9의 "강차단 모드 측정 특성을 정직하게 기록" 원칙과 정합.
- 판정·측정 로직(measureKappaN 등)은 **건드리지 말 것.** 이건 순수 표시 변경.

---

## 4. 판독값 표시 (script.js `renderReadouts`)

### 4-1. T 라벨 — 별도 캡션 줄로 분리
현재 벽 무결성 행이 `|T|=...` 값만 던지고 T가 뭔지 설명 없음.
- **T의 정의** (사용자에게 보일 캡션): T = **벽 투과율(누설 지표)**. 도선 벽에 평면파를 수직 입사시켰을 때 벽 **반대편으로 새어 나가는 장의 비율**. `modes.js wallTransmittanceT`가 계산.
- **의미**: 0에 가까울수록 도선 벽이 연속 도체판을 잘 흉내(거의 안 샘) → 모드 분해 신뢰. 크면 도선 틈으로 장이 새어 "도파관 벽" 근사가 무너짐.
- **표시 구조** — 벽 무결성 값 줄 **위 또는 아래에 작은 글씨 캡션 줄** 하나 추가:
  ```
  벽 무결성: 누설 |T|=0.146 , d/λ=0.055   [기존 경고 로직 유지]
  └ (작은 글씨) T = 도선 벽이 새는 정도. 0에 가까울수록 연속 도체판에 가까움(모드 분해 신뢰). 클수록 도선 틈으로 장이 샘.
  ```
- 기존 경고(⚠ 벽 근사 무너짐, d/λ>0.1 또는 wallT>0.35)와 "차단 κ 정확도는 |T|보다 엄격" 문구는 유지.
- CSS: 캡션 줄은 `--mut` 색, 0.78rem 정도.

### 4-2. λ·a·d 셀 값 + mm 병기
- **환산 계수: 1셀 = 1mm** (확정). a=60셀=60mm=6cm → 차단 λ_c=2a=120mm → 2.5GHz (전자레인지 대역 일치).
- **컨트롤 패널 라벨과 판독값에만** mm 병기. 캔버스 내부(픽셀=셀 1:1)와 그래프 z축은 셀 유지.
- 수정 지점 (`syncReadouts`):
  - λ: 현재 `1.50 a (90 셀)` → `1.50 a (90 셀 = 90 mm)`
  - y₀: `0.50 a` → `0.50 a (30 셀 = 30 mm)`
  - d: `4.95 셀 (d/λ=0.055)` → `4.95 셀 = 4.95 mm (d/λ=0.055)`
- **a도 어딘가에 명시**: 서브헤더나 판독 상단에 "도파관 폭 a = 60 셀 = 60 mm (= 6 cm), 차단 파장 λ_c = 2a = 120 mm ≈ 전자레인지 2.5 GHz" 한 줄 추가. 이 시뮬의 스케일 감각과 전자레인지 연결을 한 줄로 제공.
- **주의**: λ·d·a를 mm로 바꿀 때 셋 다 일관되게. λ만 바꾸고 d·a를 셀로 두면 혼란.

---

## 5. 검증 기준 (구현 후 필수 확인)

브라우저 육안 + 기존 selfTest 로그. `higher-order/index.html` 더블클릭.

1. **2×2 레이아웃**: 네 패널(입사·전체·산란·그래프)이 그리드로 뜸. 각 히트맵에 제목.
2. **공통 스케일**: 프리셋 ①에서 산란이 입사를 상쇄해 전체 내부가 옅어지는 게 세 패널 색 농도로 보임. 세 패널이 같은 스케일(입사 소스 근처만 포화).
3. **감마**: 도파관 내부 약한 모드 구조가 이전보다 또렷. 부호 대비(빨강/파랑) 유지, 상쇄가 색 약해짐으로 보임.
4. **라벨**: 세 히트맵에 '도선 벽'(상·하)·'입구 (z=0)' 라벨. 좌측 세로선의 의미가 라벨로 해소됨.
5. **그래프**: y축에 `|cₙ(z)|` 라벨. 범례에 실선/점선 샘플 획. 수치 바닥 아래 실측선이 반투명 → 이론 점선과 맞는 구간이 또렷.
6. **T 캡션**: 벽 무결성 아래(위) 작은 글씨로 T 정의 캡션. 값과 분리.
7. **mm 병기**: λ·y₀·d에 셀=mm 병기. a·λ_c·전자레인지 한 줄. 그래프 z축·캔버스는 셀 유지(혼용 없음).
8. **회귀**: 프리셋 ②③④에서 k_z·κ 측정 % 값이 이전과 동일(±15% 밴드 유지). mode2 결합이 프리셋 ④에서 0이 아닌 정상값(≈0.87)으로 뜸. → selfTest 로그로 확인.

## 6. 범위 밖 (건드리지 말 것)
- `../core.js` 수정, `kappaFitWindow` 규칙 변경.
- 측정·판정 로직(measureKappaN/measureKzN/kappaWindowN)의 수치 규칙.
- 성능 최적화(LU 재사용) — 보류 상태 유지.
- 입사/산란 3분할을 넘어선 추가 분해(n≥4 등).
