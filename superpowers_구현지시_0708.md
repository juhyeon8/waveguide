# superpowers 구현 지시 — 도선 배열 도파관 시뮬레이션

## 이 프롬프트의 위치

세 문서를 **이 우선순위로** 따른다:
1. `구현_프롬프트_도선도파관.md` — 물리 규약 §1, 코어 이식 §2, 알려진 함정 §6은 **최상위 불변 규약**. 아래 어떤 지시도 이것을 덮어쓰지 않는다.
2. `2026-07-08-도선도파관-design.md` — 화면·파일·프리셋·성능 설계. 아래 §A 수정사항이 적용된 버전으로 읽는다.
3. 본 프롬프트 — 설계 문서에 대한 **수정·보강 지시**와 작업 절차.

핵심 원칙 재확인 (위반 시 중단하고 질문): 인과 방향("경계조건이 산란파를 강제 → 상쇄·차단은 결과"), 용어(감쇠파·산란파), 차단 영역에서 c₁ 위상 수치·"위상차" 문구 절대 금지, 편광 E∥도선 고정 표기, `kappaFitWindow` 피팅 규칙 변경 금지, `core.js`(= wire_waveguide_core.js) 원본 수정 금지.

---

## A. 설계 문서 수정사항 (반드시 반영)

설계 문서의 해당 항목을 아래로 대체하거나 보강한다.

### A1. 투과율·스윕은 평면파 전용 (D5·D6 보강)
- 정보 박스의 "투과율" 표시와 ⑤ T(λ/2a) 스윕은 **평면파 입사에서만** 활성화한다.
- **선원파 입사일 때**: 투과율 라벨을 숨기고, 대신 "|c₁(x)| 감쇠 곡선 + 측정 κ"만 표시한다. ⑤ 스윕 버튼은 비활성화(회색) + 툴팁 "스윕은 평면파 입사 전용".
- 이유: 선원파는 도파관 안쪽 여기라 "밖에서 얼마나 들어오나"라는 투과 개념과 다른 물리량이다. 같은 라벨로 부르면 오해를 만든다.

### A2. 입구 앞 SWR·반사율도 평면파 전용 (설계 §2 정보 박스 보강)
- SWR·반사율 지표는 **평면파일 때만** 표시한다.
- 선원파일 때는 이 지표를 숨긴다(원천장이 x<0 축에 섞여 반사율로 해석 불가).

### A3. |c₁| 입출구 측정은 기하평균 (D6 수정)
- 입구/출구 |c₁|의 ±0.04L 구간 완화는 **산술평균이 아니라 기하평균(log 평균)**으로 한다: `exp(mean(log|c₁|))`.
- 이유: |c₁| ∝ exp(−κx) 구간에서 산술평균은 지수함수 볼록성 때문에 상향 편향된다. 기하평균은 지수 감쇠에서 무편향.

### A4. 도체판 근사 게이지 캡션 (설계 §2 보강)
- 게이지 아래 고정 캡션: "차단 영역(κ)에서 가장 민감한 지표. 전파 영역(k_z)은 성긴 벽에도 비교적 둔감하므로 누설 지표를 함께 볼 것."
- 전파 영역에서는 누설 지표(벽 위 |E| 평균)를 게이지와 동등하게 부각한다.

### A5. ④ 그래프 정규화 캡션 (설계 §2 보강)
- ④ |c₁(x)| 그래프 캡션에 한 줄: "세로축은 이 그래프 자체 정규화 — 위 패널의 색 밝기와 직접 비교하지 말 것."

### A6. 전류 위상자 매핑 규약 (설계 §2 "전류 위상자" 확정)
비어 있던 매핑을 아래로 확정한다:
- 각 도선의 순시 전류값 = `Re(c_j · e^{iφ})` = `c_j.re·cosφ − c_j.im·sinφ` (장 렌더링의 re·cosφ+im·sinφ 규약과 부호 일관되게 — 소스 항이므로 검토 후 통일).
- **표시 방식(권장)**: 점 색을 소스 부호처럼 순시값의 부호로 — 양수 빨강 계열, 음수 파랑 계열, 채도/크기 = |순시값|. 즉 도선 점이 장 색과 같은 규칙으로 깜빡이게 하여 "도선 전류가 장을 만든다"는 인과가 색으로 드러나게 한다.
- **금지**: 차단 영역에서 위상 각도 숫자·색상환 눈금값을 표기하지 않는다(규약 §1-3). 색상환 범례는 "전파 영역에서 위상 전진을 읽는 보조 도구"로만 두고, 차단 영역에서는 범례를 흐리게 처리하거나 "전 도선 동위상" 라벨로 대체.
- 목표 장면 판정: 전파 = 벽 따라 색 위상이 흘러감(파도타기) / 차단 = 전 도선이 동시에 같은 색으로 깜빡이며 안쪽으로 갈수록 옅어짐.

### A7. reference 파일은 단일 출처 (설계 §9 해소)
- 함께 제공하는 `reference/` 폴더(기존 시뮬 style.css·render.js·main.js·hankel.js 등)를 테마 색상·mm/셀 환산(=2)·색 매핑 함수(`colorForValue`)의 **단일 출처**로 삼는다.
- 값을 새로 지어내지 말 것. 색 매핑·플레이트 그리기·오버레이 로직은 기존 `render.js`를 최대한 재사용하고, 도선 배열용으로만 확장한다.

---

## B. 작업 절차 (설계 §7 검증 계획 = 프롬프트 §4 순서 준수)

각 단계 끝에서 **결과를 보고하고 멈춘다**. 다음 단계로 자동 진행하지 않는다.

1. **코어 이식 + selfTest**: `core.js`를 넣고 `node core.js` 실행, 5케이스 전체 PASS 콘솔 출력 첨부. (이미 PASS 확인됨 — 이식 후 재확인만.)
2. **콘솔 수치 검증(UI 없음)**: λ/2a = 0.75~2.0 스윕 + d ∈ {5,10,20}을 콘솔 출력. 확인 항목: (a) λ/2a=1 부근 투과 무릎, (b) 차단 κ·전파 k_z 이론 ±15%, (c) d 클수록 무릎 무뎌짐+누설 바닥 상승. 표로 보고.
3. **정적 렌더(φ=0)**: ①②③ 3패널 + 도선 점, 색 스케일 공유(입사=1 고정), ①+②=③ 육안 확인. 스크린샷.
4. **진단 패널**: ④ |c₁(x)| 그래프(측정 실선+이론 점선) + 정보 박스 수치(κ/k_z·게이지·누설·평면파일 때 투과·SWR). A1·A2 분기 동작 확인.
5. **애니메이션 + 위상자**: re·cosφ+im·sinφ 장 회전 + A6 전류 위상자. 전파 파도타기 / 차단 동위상 대비 확인. 짧은 GIF 또는 연속 스크린샷.
6. **슬라이더+경고+프리셋+스윕**: λ 범위 a연동 재클램프(D2), ±15% 경고, 프리셋 3개, ⑤ 스윕 chunked+진행률. A1대로 선원파 시 스윕 비활성.

## C. 완료 판정

`구현_프롬프트_도선도파관.md` §5 체크리스트 전체 + 설계 §7 신규 2항목 + 아래 A수정 확인:
- [ ] 선원파로 전환 시 투과율·SWR·스윕이 숨겨지거나 비활성 (A1·A2)
- [ ] 입출구 |c₁| 측정이 기하평균 (A3, 코드 확인)
- [ ] 차단 영역 어디에도 c₁ 위상 수치·"위상차" 문구 없음
- [ ] 전류 위상자가 전파=파도타기 / 차단=동위상으로 명확히 구분
- [ ] reference 값을 재사용(테마·mm규약·색매핑을 새로 짓지 않음, A7)

## D. 막히면

- 물리 규약(§1)과 구현 편의가 충돌하면: 규약을 지키고, 충돌 지점을 보고하고 멈춘다. 임의로 규약을 완화하지 않는다.
- reference 파일이 없으면 골격(index.html)과 core/field 로직까지만 진행하고, 테마·색매핑이 필요한 시점에 멈춰 파일을 요청한다.
- 성능이 예산(설계 §6)을 넘으면: 해상도·debounce를 먼저 조정하고, 그래도 안 되면 보고. Web Worker는 범위 밖(§8)이므로 도입하지 않는다.

## E. 함께 넘길 파일 체크리스트 (사용자 준비)

- [ ] `core.js` (= wire_waveguide_core.js, 원본 그대로)
- [ ] `구현_프롬프트_도선도파관.md`
- [ ] `2026-07-08-도선도파관-design.md`
- [ ] 본 프롬프트
- [ ] `reference/` : 기존 시뮬의 style.css(=style_waveguide.css)·render.js·main.js·hankel.js·physics.js·field.js·images.js·index.html — 테마·색매핑·mm규약 출처
