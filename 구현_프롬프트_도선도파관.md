# 구현 프롬프트 — 도선 배열 도파관: 입사파 + 산란파로 보는 차단

## 0. 한 문단 요약

도체 도선 두 줄로 도파관 벽을 만들고, 입사파(평면파 또는 선원파)가 도선 전류를 강제하여 산란파를 만들고, 그 산란파가 내부에서 입사파를 상쇄·재조직하는 과정을 3패널(①입사 ②산란 ③전체)로 보여주는 인터랙티브 HTML+vanilla JS 시뮬레이션을 만든다. 물리 코어는 첨부된 `wire_waveguide_core.js`(검증 완료, 자체 테스트 전체 PASS)를 **수정 없이 그대로 이식**하고, UI·렌더링만 새로 작성한다.

## 1. 물리 규약 — 협상 불가 항목

이 항목들은 구현 편의를 위해 바꾸지 않는다. 애매하면 구현 전에 질문할 것.

1. **인과 방향.** 모든 라벨·캡션·주석은 "도선 표면 E=0 조건이 산란파를 강제 → 상쇄·차단은 그 결과" 순서로 쓴다. "상쇄가 차단을 일으킨다", "산란파 때문에 막힌다" 같은 인과 역전 문장 금지.
2. **용어.** evanescent = "감쇠파" (증발장·소멸파 금지). 도선이 내는 파 = "산란파" (복사파 금지). 차단 = "차단(cutoff)".
3. **차단 영역의 위상 언어 금지.** λ > 2a에서 z(진행) 방향은 동위상 + 진폭 감쇠뿐이다. c₁(x)의 위상을 차단 영역 그래프·수치에 표시하지 않는다. "위상 반대", "위상차로 상쇄" 같은 표현을 z방향에 쓰지 않는다.
4. **편광 명시.** 시뮬레이션은 E∥도선(E_z, 2D TM) 전용이다. UI 어딘가에 "편광: E∥도선 — 이 편광에는 TEM 모드가 없음"을 고정 표기한다. 평면파 입사에 대한 캡션: "입사 평면파는 도파관 모드가 아니다. 벽 전류의 산란파가 내부에서 균일 성분을 지우고(에발트-오젠과 같은 구조), 살아남는 것은 sin(πy/a) 모드 성분뿐이다."
5. **투과율 표기.**
   - 전파 영역(λ < 2a): 전력 투과율 T = |c₁(출구)/c₁(입구)|² 를 % 로 표시.
   - 차단 영역(λ > 2a): % 하나로 요약하지 않는다. "진폭 감쇠 = ○○ dB (이 길이 L 기준)" + "길이당 감쇠 = ○○ dB/mm"로 표시. 차단 투과는 길이에 지수적이므로 길이 규약 없는 % 는 오해를 만든다.
   - 기준점 규약: 입구 |c₁|은 x = 0.12L(입구 가장자리 회절 회피), 출구 |c₁|은 x = 0.88L에서 측정. 이 규약을 캡션에 명시.
6. **누설 바닥 캡션.** 깊은 차단에서 투과가 0이 아니라 수 % 바닥에 걸린다. 이는 오차가 아니라 유한 도선 벽의 실제 누설이다. 캡션 문구: "투과가 0에 닿지 않는 것은 유한 도선 벽의 누설 때문 — 간격 d를 줄이면 바닥이 내려간다." "시뮬레이션 한계"라고 쓰지 않는다.
7. **얇은 도선 근사 유효 범위.** a_w ≪ d, a_w ≪ λ. 슬라이더 범위를 a_w ≤ d/4, a_w ≤ λ/20 안으로 제한하고, 위반 시 경고 표시.

## 2. 물리 코어 이식 규칙

`wire_waveguide_core.js`의 다음 함수를 그대로 사용한다 (파일 상단 주석에 검증 수치 있음):

| 함수 | 역할 |
|---|---|
| `buildWires(a, L, d, aw)` | 두 벽 y=±a/2, x∈[0,L] 도선 배치 |
| `incPlane(k,x,y)` / `incLine(k,xs,ys,x,y)` | 입사장 (평면파 e^{ikx} / 선원 H₀⁽¹⁾) |
| `solveMoM(wires, k, incFn)` | 경계조건 → 전류 c_j (복소 가우스 소거) |
| `totalField` / `scatteredField` | 장 계산 (③ / ② 패널) |
| `modeCoefC(...)` | n=1 모드 계수 (복소) |
| `kappaFitWindow(L, κ_thy)` + `fitKappa` | **검증된 피팅 구간 규칙 포함** — 규칙 변경 금지 |
| `measureKz(xs, phis)` | 전파 영역 k_z 측정 (위상 언랩) |
| `cutoffInfo(lambda, a)` | 이론 κ / k_z |

특히 `kappaFitWindow`의 "시작 = 0.15L, 끝 = min(0.7L, 시작 + 2.5/κ)" 규칙은 누설 바닥 오염을 막기 위해 수치 실험으로 확정된 것이다. 임의로 넓히면 κ가 계통적으로 낮게 측정된다(82.8% 사례).

렌더링 그리드 전체(예: 520×220)의 장 계산은 점별 `totalField` 호출 대신, 기존 도파관 시뮬레이션(`hankel.js`)처럼 **k·r 룩업 테이블 + 선형 보간**으로 산란파 합을 계산해 성능을 확보한다. 도선 수가 많으므로(~120개) 테이블 방식이 필수.

## 3. 화면 구성

기존 도파관 시뮬레이션(`index_waveguide.html`)의 다크 테마·패널 구조를 재사용한다.

**패널 (세로 4단):**
1. ① 입사파 — 평면파 또는 선원파만
2. ② 산란파 — 도선들의 원통파 합. 부제: "도선 표면 E=0이 강제한 산란파"
3. ③ 전체장 (①+②) — 도선 위치에 점 표시. 세 패널 같은 색 스케일(빨강 +E / 흰 0 / 파랑 −E), ①+②를 눈으로 더하면 ③이 되도록.
4. ④ 진단 그래프 — |c₁(x)| (측정 실선 + 이론 점선), 가로축은 위 패널과 동일 픽셀 매핑. 차단 시 이론 곡선 = exp(−κx), 전파 시 = 상수.

**도선 전류 위상자 표시 (핵심 신규 요소):** ②·③ 패널의 각 도선 점을 전류 위상에 따라 색상(hue) 또는 채움 정도로 표시하고, 애니메이션 위상 φ와 함께 회전시킨다. 목표 장면: 전파 영역에서는 벽을 따라 전류 위상이 전진(파도타기), 차단 영역에서는 **모든 도선 동위상·진폭만 감쇠**. 이 대비가 이 시뮬레이션의 교육적 핵심 장면이므로 반드시 눈에 띄게 구현.

**컨트롤 (왼쪽 사이드바):**
- 파장 λ 슬라이더 (mm 표기, 기존 시뮬과 동일한 mm/셀=2 규약)
- 도파관 간격 a 슬라이더 (2a = 차단 파장 병기)
- 도선 간격 d 슬라이더
- 도파관 길이 L 슬라이더
- 입사 형태 토글: [평면파 (입구 결합)] / [선원파 (내부 여기, 입구 앞 x=−80 위치)]
- 애니메이션: 재생/일시정지, 속도

**정보 박스 (사이드바 하단):**
- 차단/전파 배지: "차단: λ > 2a → 감쇠파" / "전파: λ < 2a → 모드 진행"
- 이론 κ 또는 k_z, 측정값, 백분율. **±15% 밖이면 주황 경고 + "d 감소 권장"**
- "도체판 근사 게이지": 측정 κ/이론 κ (또는 k_z 비)를 막대 게이지로 — "이 도선 벽은 지금 몇 % 도체판인가"
- 투과율 (규약 5번대로: 전파 시 %, 차단 시 dB + dB/mm)
- 도선 사이 중간점 |E| 평균 (누설 지표)
- 입구 앞 정재파: x<0 축 위 |E|의 최대/최소 비(SWR)와 반사율 추정 — 캡션: "차단 시 입사 에너지는 흡수되지 않고 반사된다. 입구 앞 정재파가 그 증거."

## 4. 구현 순서 (물리 확정 → 숫자 → 그림)

각 단계 완료 시 결과를 보고하고 다음 단계로. 단계를 건너뛰지 않는다.

1. **코어 이식 + 자체 테스트**: `wire_waveguide_core.js`를 프로젝트에 넣고 `node`로 selfTest 전체 PASS 확인.
2. **콘솔 수치 검증**: UI 없이 λ 스윕(λ/2a = 0.75~2.0)을 돌려 투과 곡선·κ·k_z를 콘솔 출력, 아래 §5 기준 통과 확인.
3. **정적 렌더링**: 위상 φ=0 고정으로 3패널 + 도선 점 렌더. 색 스케일 공유 확인.
4. **진단 패널**: |c₁(x)| 그래프 + 이론 점선 + 정보 박스 수치.
5. **애니메이션 + 전류 위상자**: re·cosφ + im·sinφ 규약(기존 시뮬과 동일), 도선 위상자 회전.
6. **슬라이더 연결 + 경고 로직**: 재계산은 슬라이더 input마다 (MoM 풀이 ~100ms 이내 목표, 초과 시 debounce).

## 5. 완료 판정 기준 (Acceptance)

- [ ] selfTest 5케이스 전체 PASS
- [ ] 기본 파라미터(a=60, L=300, d=5, a_w=0.8 셀)에서 측정 κ, k_z 모두 이론의 ±15% 이내
- [ ] λ 슬라이더로 λ/2a=1을 지날 때 배지·투과 표시·감쇠 곡선이 일관되게 전환
- [ ] 차단 영역에서 ①패널은 내부에 파동이 가득한데 ③패널 내부는 어두움 (상쇄가 눈으로 확인됨)
- [ ] 전파/차단에서 도선 전류 위상자가 각각 "위상 전진 / 전체 동위상"으로 보임
- [ ] 입구 앞(x<0)에 차단 시 뚜렷한 정재파 무늬
- [ ] d를 키우면: 도체판 근사 게이지 하락 + 누설 지표 상승 + κ 경고 발동
- [ ] 차단 영역 어디에도 c₁ 위상 수치·"위상차" 문구 없음 (§1-3)
- [ ] 모든 캡션이 §1의 인과 방향·용어 규약 준수

## 6. 알려진 함정 (테스트에서 이미 확인됨)

- κ 피팅 구간을 넓히면 누설 바닥 때문에 κ가 낮게 나온다 → `kappaFitWindow` 규칙 고정.
- 입구·출구 가장자리 0.1L 이내는 회절로 |c₁|이 요동한다 → 측정·정규화 기준점을 안쪽으로.
- d=10부터 κ 오차가 15%를 넘기 시작한다(119% 관측) → 기본값 d=5, 경고 로직 필수.
- 평면파 입사 시 도파관 바깥에도 평면파가 존재한다(벽 바깥을 스치며 진행). 이는 오류가 아니라 물리다 — ①패널에서 그대로 보이게 두고, 혼동 방지 캡션 한 줄 추가.
- 선원파 모드에서 선원 위치(x=−80, y=0)가 렌더 창 밖이면 점 표시를 생략하되 캡션에 위치 명시.

## 7. 파일 구성

기존 도파관 시뮬레이션 구조를 따른다:
`index.html` / `style.css` / `core.js`(= wire_waveguide_core.js 원본 그대로) / `table.js`(Hankel 룩업) / `render.js` / `main.js`. 한국어 UI, 주석도 한국어.
